[build]
  publish = "."
  command = ""

# Enable Netlify Identity + Git Gateway so the CMS admin can log in
# and commit content changes directly to GitHub without touching code.
[build.environment]
  NODE_VERSION = "18"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options       = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"

# Allow the admin panel to load Decap CMS from CDN
[[headers]]
  for = "/admin/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://unpkg.com https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://unpkg.com https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https:; connect-src 'self' https://api.netlify.com https://auth.netlify.com https://*.netlify.app"

# Cache static assets
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# Always serve fresh content.json so CMS edits are reflected immediately
[[headers]]
  for = "/content.json"
  [headers.values]
    Cache-Control = "public, max-age=60, s-maxage=60"
